#pragma once

#include <cmath>

constexpr float EPSILON = 0.0001;

constexpr int PPM_LINE_SIZE = 4; /* 4 * ~11 ~= 44 chars*/

constexpr int TUPLE_HEIGHT = 4;
constexpr int TUPLE_WIDTH = 4;

constexpr float PI = std::acos(-1);
